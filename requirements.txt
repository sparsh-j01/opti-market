# Parity oracle (CI/testing only — never hosted)
fastapi>=0.110,<1
uvicorn>=0.27,<1
pydantic>=2.6,<3

# Math / data
numpy>=1.26,<3
pandas>=2.1,<3
scipy>=1.11,<2

# Build-time treasury refresh only (scripts/refresh_treasury.py)
yfinance>=0.2,<2

# Tests
pytest>=8,<9
httpx>=0.27,<1
