# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.venv/
venv/
env/
*.egg
.pytest_cache/
htmlcov/
.coverage

# Node
node_modules/
.next/
out/

# Vendored Pyodide runtime — regenerated at build time, never committed
frontend/public/pyodide/
frontend/pyodide-*.tar.bz2

# Playwright
test-results/
playwright-report/

# NOTE: frontend/public/py and frontend/public/data ARE committed — Vercel's
# Node build has no Python to regenerate them; the refresh-data Action keeps
# them in sync. Root copies remain the canonical source of truth.

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Environment — NEVER commit API keys or secrets
.env
.env.local
.env.*.local

# Cache
market_data.csv

# Sample files (not part of the project)
*.pdf

# Presentation & Report (local only)
presentation.html
report.html
report.docx
ppt_content.md

# Gemini
.gemini/

# gstack — local session state, security reports, learnings (never commit)
.gstack/

# Planning docs (local only)
plan.md
